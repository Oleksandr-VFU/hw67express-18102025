extends ../users/layout

block content
  h2 Вхід

  if error
    .alert.alert-error
      p= error

  form(action="/auth/login", method="POST", class="auth-form")
    .form-group
      label(for="username") Ім'я користувача:
      input(
        type="text", 
        id="username", 
        name="username", 
        value=(formData && formData.username) || '',
        required
      )

    .form-group
      label(for="password") Пароль:
      input(
        type="password", 
        id="password", 
        name="password", 
        required
      )

    .form-actions
      button(type="submit", class="btn btn-primary") Увійти
      a(href="/auth/register", class="btn btn-secondary") Немає акаунту? Зареєструватися

  .demo-users
    h3 Демо користувачі для тестування:
    ul
      li
        strong admin
        |  / admin123 - адміністратор (доступ до статей)
      li
        strong user
        |  / user123 - звичайний користувач